"""package-research — the Memory Doctrine's consuming tool.

Turns a folder of raw notes/research into a doctrine-grounded knowledge
package (KPM) via the full pipeline:
ingest -> distill -> score -> verify -> split -> assemble -> validate.
The ``package-research`` CLI lives in :mod:`package_research.cli`.
"""

__version__ = "0.1.0"
